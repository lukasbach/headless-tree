import { FeatureImplementation } from "../../types/core";
import { SelectionFeatureDef } from "./types";

export const selectionFeature: FeatureImplementation<SelectionFeatureDef<any>> =
  {
    key: "selection",
  };
